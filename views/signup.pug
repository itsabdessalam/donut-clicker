extends layout

block content
    .main.signup
        .header-text
            img(src="/images/donut1.png", alt="",width="100px")
            h1(class="header-title") Donut Clicker
            small Built with NodeJS
        .valign-wrapper.row.login-box
            .col.card.hoverable.s10.pull-s1.m6.pull-m3.l4.pull-l4
                form(method="POST")
                    .card-content
                        if errors
                            p.message_error
                                span.error_icon
                                    i.ion-ios-close-outline 
                                | All fields are required
                            each error in errors
                                li= error.msg 
                        span.card-title.left-align Sign up
                        .row
                            .input-field.col.s12
                                label(for="username") Username
                                input#username.validate(type="text" name="username")
                            .input-field.col.s12
                                label(for="email") Email
                                input#email.validate(type="email" name="email")
                            .input-field.col.s12
                                label(for="password") Password
                                input#password.validate(type="password" name="password")
                            .input-field.col.s12
                                label(for="password") Confirm password
                                input#password_c.validate(type="password" name="password_c")
                            .input-field.col.s12
                                input.btn.btn-custom.waves-effect.waves-light(type="submit" value="Sign up")
                    .card-action
                        a.link-left  Have an account ?
                        a(class="link-right" , href="/users/login") Log in
